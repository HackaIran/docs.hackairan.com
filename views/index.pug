extends layout

block head
  link(rel="stylesheet", href="/stylesheets/home.css")

block content
  
  .column.tags
  .column.titles
    .header
      input(type="text", placeholder="Search Documents")
    ul
      - for (let document of documents) {
        li(data-id=document.id)
          h3= document.title
          span
            time= document.date
            span  | 
            span= document.creator
          p= document.abstract
      - }
  .column.content

